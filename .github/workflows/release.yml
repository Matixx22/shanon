name: release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - os: macos-latest
            target: aarch64-apple-darwin
    runs-on: ${{ matrix.os }}
    # Release binaries must be built by current stable, not by the MSRV.
    # rust-toolchain.toml pins the repository to 1.97 and outranks
    # `rustup default`, so without this the shipped artifacts would silently be
    # MSRV builds. An environment variable outranks the file.
    env:
      RUSTUP_TOOLCHAIN: stable
    steps:
      - uses: actions/checkout@v7

      - name: Install Rust toolchain
        run: |
          rustup toolchain install stable --profile minimal
          rustup target add ${{ matrix.target }}
          rustup default stable

      - name: Show active toolchain
        run: rustc --version --verbose

      - uses: Swatinem/rust-cache@v2

      - name: Build release binary
        run: cargo build --release --locked -p shanon-cli --target ${{ matrix.target }}

      - name: Package
        env:
          REF_NAME: ${{ github.ref_name }}
          TARGET: ${{ matrix.target }}
        run: |
          archive="shanon-${REF_NAME}-${TARGET}.tar.gz"
          tar -C "target/${TARGET}/release" -czf "$archive" shanon
          shasum -a 256 "$archive" > "$archive.sha256"

      - uses: actions/upload-artifact@v7
        with:
          name: shanon-${{ matrix.target }}
          path: |
            shanon-*.tar.gz
            shanon-*.tar.gz.sha256

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - uses: actions/download-artifact@v8
        with:
          path: dist
          merge-multiple: true
      - name: Publish GitHub release
        uses: softprops/action-gh-release@v3
        with:
          files: |
            dist/*.tar.gz
            dist/*.tar.gz.sha256
          generate_release_notes: true
