# Rust build artifacts
/target/

# Never commit client data, collections, or mapping files.
*.zip
*.map.json
scratch/
sharphound-samples/

# The parity replay seed carries only a fixed synthetic salt — safe to track.
!tests/parity/seed.map.json

# Parity golden corpus — raw pre-anonymization lab AD collections + reversal
# maps. Kept LOCAL, never committed. The replay tests self-skip without it.
tests/parity/golden/
